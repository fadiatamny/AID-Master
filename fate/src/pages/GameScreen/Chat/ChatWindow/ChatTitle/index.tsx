import styles from './styles.module.css'

const ChatTitle = () => {
    return (
        <div className={`${styles.container}`}>
            <p className={styles.title}>Your Adventure</p>
        </div>
    )
}

export default ChatTitle
